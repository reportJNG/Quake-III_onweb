{
  "name": "openarena-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/verify-runtime-assets.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node --check vite.config.js && node --check src/main.js && node --check src/engine.js && node --check src/config.js && node --check src/resolution.js && node --check scripts/manifest.mjs && node --check scripts/write-manifest.mjs && node --check scripts/verify-runtime-assets.mjs && node --check scripts/build-slim-assets.mjs && node --check scripts/smoke-game.mjs",
    "test": "node --test test/*.test.js",
    "verify": "npm run check && npm test && npm run build",
    "slim:assets": "node scripts/build-slim-assets.mjs",
    "smoke:game": "node scripts/smoke-game.mjs",
    "prepare:assets": "powershell -ExecutionPolicy Bypass -File scripts/prepare-assets.ps1",
    "build:engine": "powershell -ExecutionPolicy Bypass -File scripts/build-engine.ps1",
    "setup": "npm run prepare:assets && npm run build:engine"
  },
  "devDependencies": {
    "adm-zip": "0.6.0",
    "vite": "8.1.3"
  }
}
